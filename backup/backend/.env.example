# AWS credentials are supplied only by the environment or an IAM role; never commit values.
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SNS_TOPIC_ARN=
# Optional: defaults to <repository>/labs
LABS_DIRECTORY=
